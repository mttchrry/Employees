"use strict";
require('dotenv').config();

// copy this file into one named config.env.js and fill out the section below.
module.exports = {
  firebaseEndpoint: "",
  firebaseKey: "",
  firebaseSenderId: "",
};
